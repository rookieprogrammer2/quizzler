class Quiz {
  String id;
  String title;
  DateTime date;
  String question;
  List<String> answers;

  Quiz({
    required this.id,
    required this.title,
    required this.date,
    required this.question,
    required this.answers,
  });

// Add other properties and methods as needed
}
